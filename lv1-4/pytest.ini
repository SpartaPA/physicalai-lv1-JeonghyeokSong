[pytest]
# ROS2 가 PYTHONPATH 로 밀어넣는 pytest 플러그인이 최신 pytest 와 충돌해
# 수집 단계에서 INTERNALERROR 로 죽는 것을 막는다 (launch_testing / launch_ros).
addopts = -p no:launch_testing -p no:launch_ros -p no:launch_pytest
